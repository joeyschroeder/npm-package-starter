import { yourPackage } from './your-package';

describe('dateInputPropType', () => {
  it('should return "your package goes here."', () => {
    const result = yourPackage();
    expect(result).toEqual('your package goes here.');
  });
});
